# Example source

`source/GreetingFormatter.cs` is intentionally small and contains no external
dependencies. The default documentation space uses its named marker to
demonstrate snippet lifting and restricts the code browser to this example
directory and the `.cs` extension.

Keep example files generic, deterministic, and safe to publish.
